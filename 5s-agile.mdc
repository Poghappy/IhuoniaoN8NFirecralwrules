---
description: 5S敏捷开发个人规则 - 规范个人执行细节
alwaysApply: true
---

# 5S敏捷开发个人规则

> 专治"AI依赖症"和"开发拖延症"的个人执行规范

## 1S：文档管理（核心中的核心）

### 创建时机
- 新项目第一天必须建说明文档.md
- 包含：项目规划 + 实施方案 + 进度记录

### 更新要求
- **每次开发前先读文档**：确保了解当前项目状态
- **改一行代码就同步更新文档**：保持文档与代码一致
- **完成任务后必须写"结果说明"**：记录完成情况、遇到的问题、解决方案

### 文档格式要求
```markdown
# 项目说明文档

## 项目规划
- 项目目标
- 技术栈
- 预期时间

## 实施方案
- 架构设计
- 技术选型
- 开发步骤

## 进度记录
- [日期] 完成内容
- [日期] 遇到问题及解决方案
- [日期] 下一步计划
```

## 2S：开发流程（顺序化思考，拒绝跳步）

### 开发顺序
严格按照以下顺序执行，**绝不允许跳步**：

1. **先写接口文档**（输入参数、返回格式）
2. **再写单元测试**（边界条件：空列表、分页越界、异常情况）
3. **最后实现功能**

### 关键原则
- **完成一个任务打勾一个**：绝不允许"这个功能差不多了，先做下个"
- **禁止同时修改多个文件**：专注一个任务，完成后再进行下一个
- **任务必须可独立测试**：每个功能模块都应该有对应的测试

### 任务管理示例
```markdown
## 任务列表

- [ ] 1. 用户登录接口
  - [ ] 1.1 编写接口文档
  - [ ] 1.2 编写单元测试
  - [ ] 1.3 实现功能代码
  - [ ] 1.4 运行测试验证

- [ ] 2. 用户注册接口
  - [ ] 2.1 编写接口文档
  - [ ] 2.2 编写单元测试
  - [ ] 2.3 实现功能代码
  - [ ] 2.4 运行测试验证
```

## 3S：问题解决（官方文档是爹，搜索引擎是儿子）

### 问题解决优先级
1. **首先**：查阅官方文档（Python查python.org，Java查Oracle文档）
2. **其次**：使用搜索引擎查找解决方案
3. **最后**：询问AI助手

### 严禁行为
- ❌ 严禁"百度一下复制粘贴"：必须理解解决方案的原理
- ❌ 严禁使用非官方方案：优先使用官方推荐的最佳实践
- ❌ 严禁跳过文档直接搜索：官方文档是权威来源

### 技术文档查找指南
- **Python**: https://docs.python.org/
- **JavaScript/TypeScript**: https://developer.mozilla.org/
- **Node.js**: https://nodejs.org/docs/
- **Firecrawl**: https://docs.firecrawl.dev/
- **Cursor**: https://cursor.com/docs

## 4S：执行约束（三大"绝不允许"）

### 约束1：绝不允许项目延期
- **提前3天识别风险**：主动发现潜在问题
- **延期必须同步更新文档并说明原因**：记录延期原因和影响

### 约束2：绝不允许超出计划
- **加功能可以**：但必须先改规划文档
- **评估影响**：分析新功能对现有系统的影响
- **更新进度**：调整项目时间表

### 约束3：绝不允许出错
以下三类错误零容忍：
1. **编译错误**：代码必须能正常编译运行
2. **测试不通过**：所有测试用例必须通过
3. **文档不一致**：文档必须与代码保持一致

## 5S：环境与输出（细节决定成败）

### 开发环境规范
- **统一开发环境**：全团队使用相同版本的工具
- **IDE插件版本锁定**：例如Prettier用2.8.0，避免版本差异
- **统一操作系统**：避免路径分隔符等问题（Windows用`\`，Linux/macOS用`/`）

### 代码输出规范

#### 函数注释格式
所有函数必须包含完整注释，格式如下：

```python
def login(username: str, password: str) -> dict:
    """
    用户登录接口
    
    :param username: 用户名（长度6-20位，仅支持字母数字）
    :param password: 密码（包含大小写+数字，长度8-32位）
    :return: {
        'code': int,  # 0表示成功，非0表示失败
        'data': {
            'token': str,  # JWT令牌
            'user': dict   # 用户信息字典
        },
        'message': str  # 错误信息（失败时返回）
    }
    :raises ValueError: 当用户名或密码格式不正确时
    :raises AuthenticationError: 当用户名或密码错误时
    """
    pass
```

#### 类注释格式
```python
class NewsCollector:
    """
    新闻采集器类
    
    负责从Firecrawl API获取新闻数据并处理
    
    属性:
        api_key (str): Firecrawl API密钥
        base_url (str): API基础URL
        timeout (int): 请求超时时间（秒）
    
    示例:
        >>> collector = NewsCollector(api_key="fc-xxx")
        >>> news = collector.collect_news("人工智能")
    """
    pass
```

#### 代码风格要求
- **变量命名**：使用有意义的名称，遵循PEP8规范
- **类型注解**：所有函数参数和返回值必须有类型注解
- **异常处理**：必须包含适当的异常处理逻辑
- **代码注释**：复杂逻辑必须添加注释说明

## 执行检查清单

每次开发前检查：
- [ ] 已阅读项目文档
- [ ] 已理解任务需求
- [ ] 已规划实现步骤
- [ ] 已准备测试用例

每次开发后检查：
- [ ] 代码已通过测试
- [ ] 文档已同步更新
- [ ] 代码已添加注释
- [ ] 已记录完成情况

## 常见错误预防

### 错误1：跳步开发
- ❌ 直接写代码，不写测试
- ✅ 先写测试，再写代码

### 错误2：文档滞后
- ❌ 代码改了，文档忘记更新
- ✅ 改代码时同步更新文档

### 错误3：依赖非官方方案
- ❌ 直接搜索复制粘贴代码
- ✅ 先查官方文档，理解原理后再实现

### 错误4：忽略边界条件
- ❌ 只测试正常情况
- ✅ 测试正常、异常、边界情况

## 质量保证

- **代码审查**：提交前必须自我审查
- **测试覆盖**：关键功能必须有单元测试
- **文档完整**：所有公开接口必须有文档说明
- **错误处理**：所有可能的错误情况都要处理
