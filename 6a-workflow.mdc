---
description: 6A工作流项目规则 - 标准化的AI开发流程管理
alwaysApply: true
---

# 6A工作流项目规则

> 基于TRAE的6A工作流体系，为AI开发提供标准化流程管理

## 工作流概述

6A工作流（Align-Architect-Atomize-Approve-Automate-Assess）是标准化的项目规则体系，通过六个阶段确保AI开发的质量和效率。

## 阶段1：Align（对齐）

**目标**：把模糊需求锤成"钢印级"规范，杜绝AI"我以为"式开发

**执行要求**：
- 需求边界必须清晰明确
- 每个需求必须包含验收标准
- 禁止在需求不明确的情况下开始编码
- 需求确认后，必须生成 `REQUIREMENTS_任务名.md` 文档

**输出文档**：
- `REQUIREMENTS_任务名.md` - 包含需求描述、边界条件、验收标准

## 阶段2：Architect（架构）

**目标**：从共识文档到系统架构，拒绝AI"拍脑袋写代码"

**执行要求**：
- Align阶段确认需求后，自动生成 `DESIGN_任务名.md`
- 必须使用mermaid画架构图
- 必须明确数据流、接口定义、模块划分
- 架构设计必须与现有系统对齐

**输出文档**：
- `DESIGN_任务名.md` - 包含架构图、接口定义、数据模型

**架构图示例格式**：
```mermaid
graph TD
    A[需求] --> B[架构设计]
    B --> C[模块划分]
    C --> D[接口定义]
```

## 阶段3：Atomize（原子化）

**目标**：拆分任务到AI"不可能失败"的粒度

**执行要求**：
- 复杂任务必须拆分成20行内的代码块
- 每个原子任务必须独立可测试
- 任务拆分必须生成 `TASK_任务名.md`

**输出文档**：
- `TASK_任务名.md` - 包含任务列表、验收标准、依赖关系

**任务拆分原则**：
- 单个函数不超过20行
- 每个任务有明确的输入输出
- 任务之间依赖关系清晰

## 阶段4：Approve（审批）

**目标**：确保验收标准可测试、可验证

**执行要求**：
- 必须拿着 `TASK_任务名.md` 逐条检查
- 验收标准必须可测试（不能写"功能正常"，必须写具体测试条件）
- 所有验收标准必须明确：输入、预期输出、边界条件

**验收标准示例**：
- ❌ 错误："用户注册接口功能正常"
- ✅ 正确："输入重复手机号返回code=1001错误，输入格式正确返回code=0并生成token"

## 阶段5：Automate（自动化执行）

**目标**：AI按任务顺序执行，确保代码质量

**执行要求**：
- 严格按照TASK文档中的任务顺序执行
- 每写完一个函数必须先写单元测试
- 测试不通过不准提交代码
- 每个函数必须包含完整的类型注解和文档字符串

**代码规范**：
```python
def login(username: str, password: str) -> dict:
    """
    用户登录接口
    
    :param username: 用户名（长度6-20位）
    :param password: 密码（包含大小写+数字）
    :return: {code:0,data:{token:str,user:dict}}
    """
    # 实现代码
    pass
```

## 阶段6：Assess（评估）

**目标**：评估代码质量，生成最终报告

**执行要求**：
- 生成 `FINAL_任务名.md` 文档
- 包含代码质量评分（SonarQube扫描）
- 测试覆盖率要求≥80%
- 列出遗留TODO和后续优化建议

**输出文档**：
- `FINAL_任务名.md` - 包含质量评估、测试覆盖率、TODO列表

## 执行约束

### 三大"绝不允许"
1. **绝不允许项目延期**：提前3天识别风险，延期必须同步更新文档并说明原因
2. **绝不允许超出计划**：加功能可以，但必须先改规划文档，评估影响
3. **绝不允许出错**：编译错误、测试不通过、文档不一致，这三类错误零容忍

## 文档管理要求

### 创建时机
- 新项目第一天必须建说明文档.md，包含"项目规划+实施方案+进度记录"

### 更新要求
- 每次开发前先读文档
- 改一行代码就同步更新文档
- 完成任务后必须写"结果说明"

## 项目文件结构

```
项目根目录/
├── REQUIREMENTS_任务名.md    # 需求文档
├── DESIGN_任务名.md          # 架构设计文档
├── TASK_任务名.md            # 任务拆分文档
├── FINAL_任务名.md           # 最终评估文档
└── 说明文档.md               # 项目总体说明
```

## 质量指标

- **返工率目标**：<5%
- **测试覆盖率要求**：≥80%
- **代码质量问题**：<3个/千行（SonarQube扫描）
- **文档完整性**：100%

## 注意事项

- 严格按照6A流程执行，不得跳步
- 每个阶段完成后再进入下一阶段
- 文档必须与代码同步更新
- 遇到问题先查官方文档，禁止随意搜索解决方案
