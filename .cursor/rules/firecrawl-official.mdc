# Firecrawl 官方文档规则

## 📋 概述

本规则文件基于 [Firecrawl 官方文档](https://docs.firecrawl.dev/introduction) 制定，确保代码实现与官方 API 保持一致。

## 🔧 Firecrawl SDK 使用规范

### 1. SDK 导入

**推荐方式（v2 SDK）**:
```python
from firecrawl import Firecrawl

firecrawl = Firecrawl(api_key="fc-YOUR-API-KEY")
```

**兼容旧版本**:
```python
try:
    from firecrawl import Firecrawl
except ImportError:
    from firecrawl import FirecrawlApp as Firecrawl
```

### 2. API 方法调用

#### 2.1 Scrape（抓取）

**正确用法**:
```python
# 基本用法
doc = firecrawl.scrape("https://firecrawl.dev", formats=["markdown", "html"])

# 带参数
doc = firecrawl.scrape(
    url="https://example.com",
    formats=["markdown"],
    actions=[
        {"type": "write", "text": "john@example.com"},
        {"type": "click", "selector": 'button[type="submit"]'},
        {"type": "wait", "milliseconds": 1500},
    ],
)
```

**响应格式**:
```python
{
    "success": True,
    "data": {
        "markdown": "...",
        "html": "...",
        "metadata": {...}
    }
}
```

#### 2.2 Crawl（爬取）

**正确用法**:
```python
# 使用 SDK（自动等待完成）
docs = firecrawl.crawl(url="https://docs.firecrawl.dev", limit=10)

# 使用 API（返回 ID，需要检查状态）
result = firecrawl.start_crawl(url="https://docs.firecrawl.dev", limit=10)
crawl_id = result["id"]
status = firecrawl.get_crawl_status(crawl_id)
```

**响应格式**:
```python
{
    "status": "completed",
    "total": 36,
    "completed": 36,
    "data": [...]
}
```

#### 2.3 Extract（提取）

**正确用法**:
```python
# 使用 schema
result = firecrawl.scrape(
    'https://firecrawl.dev',
    formats=[{
        "type": "json",
        "schema": {...}
    }]
)

# 使用 prompt（无 schema）
result = firecrawl.scrape(
    'https://firecrawl.dev',
    formats=[{
        "type": "json",
        "prompt": "Extract the company mission from the page."
    }]
)
```

#### 2.4 Search（搜索）

**正确用法**:
```python
results = firecrawl.search(
    query="firecrawl",
    limit=3,
    sources=["web", "images", "news"]
)
```

### 3. 参数规范

#### 3.1 formats 参数

**支持的值**:
- `"markdown"` - Markdown 格式
- `"html"` - HTML 格式
- `"screenshot"` - 截图
- `{"type": "json", "schema": {...}}` - JSON 结构化数据
- `{"type": "json", "prompt": "..."}` - JSON 无模式提取

#### 3.2 actions 参数

**支持的操作类型**:
- `{"type": "wait", "milliseconds": 1500}` - 等待
- `{"type": "click", "selector": "button"}` - 点击
- `{"type": "write", "text": "text"}` - 输入文本
- `{"type": "press", "key": "Tab"}` - 按键
- `{"type": "scroll", "direction": "down"}` - 滚动
- `{"type": "screenshot", "fullPage": True}` - 截图

**重要提示**: 在执行其他操作之前/之后几乎总是使用 `wait` 操作。

#### 3.3 timeout 参数

- **单位**: 毫秒（ms）
- **默认值**: 30000（30秒）
- **示例**: `timeout=120000` 表示 120 秒

### 4. 错误处理

**标准错误处理模式**:
```python
try:
    result = firecrawl.scrape(url)
    if result and result.get("success"):
        data = result["data"]
        # 处理数据
    else:
        logger.error(f"抓取失败: {url}")
except Exception as e:
    logger.error(f"抓取时发生错误: {e}")
```

### 5. 响应数据结构

#### 5.1 Scrape 响应

```python
{
    "success": True,
    "data": {
        "markdown": str,
        "html": str,
        "metadata": {
            "title": str,
            "description": str,
            "language": str,
            "sourceURL": str,
            "statusCode": int,
            ...
        }
    }
}
```

#### 5.2 Crawl 响应

```python
{
    "status": "completed" | "scraping",
    "total": int,
    "completed": int,
    "creditsUsed": int,
    "next": str | None,  # 分页 URL
    "data": [
        {
            "markdown": str,
            "html": str,
            "metadata": {...}
        },
        ...
    ]
}
```

### 6. 最佳实践

#### 6.1 使用缓存

```python
# 使用 maxAge 参数（毫秒）
result = firecrawl.scrape(
    url="https://example.com",
    maxAge=172800000  # 2天缓存
)
```

#### 6.2 并发控制

```python
import asyncio
from asyncio import Semaphore

semaphore = Semaphore(5)  # 最多5个并发请求

async def scrape_with_limit(url):
    async with semaphore:
        return firecrawl.scrape(url)
```

#### 6.3 重试机制

```python
import time
from typing import Callable

def retry_operation(operation: Callable, max_retries: int = 3):
    for attempt in range(max_retries):
        try:
            return operation()
        except Exception as e:
            if attempt == max_retries - 1:
                raise
            time.sleep(2 ** attempt)  # 指数退避
```

### 7. 代码检查清单

在编写 Firecrawl 相关代码时，请确保：

- [ ] 使用正确的 SDK 导入方式（`Firecrawl` 而非 `FirecrawlApp`）
- [ ] `formats` 参数格式正确（列表或字典）
- [ ] `timeout` 参数单位为毫秒
- [ ] `actions` 中包含必要的 `wait` 操作
- [ ] 正确处理响应数据结构（检查 `success` 字段）
- [ ] 实现适当的错误处理和重试机制
- [ ] 使用缓存提高性能（`maxAge` 参数）

### 8. 参考资源

- [官方文档](https://docs.firecrawl.dev/introduction)
- [API 参考](https://docs.firecrawl.dev/api-reference/v2-introduction)
- [Python SDK](https://docs.firecrawl.dev/sdks/python)
- [Node SDK](https://docs.firecrawl.dev/sdks/node)

---

**最后更新**: 2025-01-27
**基于**: Firecrawl v2 官方文档
