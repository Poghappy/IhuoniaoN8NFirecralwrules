---
description: Cursor AI生成Markdown文件的强制规范 - 确保生成的内容符合markdownlint规则
globs: ["**/*.md"]
alwaysApply: true
---

# Cursor AI Markdown生成强制规范

> 本规则确保AI生成的Markdown内容自动符合项目的markdownlint配置

## 🎯 核心原则

**AI在生成或编辑任何Markdown文件时，必须严格遵守以下规则**

## 📋 必须遵守的格式规则

### 1. 标题格式（MD003/MD022）

**强制要求**：

- ✅ 使用ATX风格标题：`## 标题`
- ✅ 标题前后必须有空行
- ❌ 禁止使用下划线风格：`标题\n===`

**示例**：

```markdown
## 正确的标题格式

这是内容段落。

### 子标题

这是子标题下的内容。
```

### 2. 代码块格式（MD040/MD046）

**强制要求**：

- ✅ 使用围栏式代码块（三个反引号）
- ⚠️ 代码块可以不指定语言（MD040已禁用）
- ✅ 代码块前后必须有空行

**示例**：

```markdown
这是文本内容。

\`\`\`
代码内容（可以不指定语言）
\`\`\`

继续文本内容。
```

或者指定语言：

```markdown
这是文本内容。

\`\`\`python
def hello():
    print("Hello")
\`\`\`

继续文本内容。
```

### 3. 列表格式（MD032）

**强制要求**：

- ✅ 列表前后必须有空行
- ✅ 列表项使用 `-` 开头
- ✅ 列表项与标记之间有一个空格

**示例**：

```markdown
这是段落。

- 列表项1
- 列表项2
- 列表项3

这是下一段落。
```

### 4. 链接格式（MD034）

**灵活处理**：

- ✅ 允许裸露URL（MD034已禁用）
- ✅ 推荐使用Markdown链接：`[文本](URL)`
- ✅ 也可以直接写URL：`https://example.com`

**示例**：

```markdown
推荐格式：[访问官网](https://example.com)

也可以直接写：https://example.com
```

### 5. 行长度限制（MD013）

**灵活处理**：

- ⚠️ 标题和代码块不受行长度限制
- ⚠️ 普通文本建议不超过120字符（但不强制）
- ✅ 长URL和表格不受限制

### 6. HTML标签（MD033）

**部分允许**：

- ✅ 允许使用：`<br>`, `<sub>`, `<sup>`, `<details>`, `<summary>`
- ❌ 其他HTML标签尽量避免

## 🔧 AI生成Markdown的检查清单

**每次生成或编辑Markdown文件后，AI必须自检**：

- [ ] 所有标题使用 `##` ATX风格
- [ ] 标题前后都有空行
- [ ] 代码块使用三个反引号
- [ ] 代码块前后都有空行
- [ ] 列表前后都有空行
- [ ] 列表项格式正确：`- 项目`
- [ ] 段落之间有空行
- [ ] 文件末尾有换行符

## 🚫 零容忍错误

以下错误**绝不允许**出现：

1. ❌ 标题使用下划线格式
2. ❌ 标题前后没有空行
3. ❌ 代码块前后没有空行
4. ❌ 列表前后没有空行
5. ❌ 连续3个以上空行（MD012）

## 💡 实用技巧

### 标题层级

```markdown
# 一级标题（文档标题）

## 二级标题（主要章节）

### 三级标题（子章节）

#### 四级标题（详细说明）
```

### 混合列表

```markdown
有序列表：

1. 第一步
2. 第二步
3. 第三步

无序列表：

- 选项A
- 选项B
- 选项C
```

### 嵌套列表

```markdown
- 主列表项1
  - 子列表项1.1
  - 子列表项1.2
- 主列表项2
  - 子列表项2.1
```

## 📝 常见场景示例

### 场景1：编写技术文档

```markdown
## 安装步骤

按照以下步骤安装：

1. 克隆仓库
2. 安装依赖
3. 运行项目

安装命令：

\`\`\`bash
git clone https://github.com/example/repo.git
cd repo
npm install
\`\`\`

完成后访问：http://localhost:3000
```

### 场景2：编写API文档

```markdown
## 用户登录接口

**接口地址**：`POST /api/login`

**请求参数**：

- username: 用户名
- password: 密码

**响应示例**：

\`\`\`json
{
  "code": 0,
  "data": {
    "token": "xxx"
  }
}
\`\`\`
```

### 场景3：编写项目说明

```markdown
## 项目简介

这是一个新闻采集系统。

## 主要功能

- 新闻抓取
- 数据处理
- API接口

## 技术栈

使用以下技术：

- Python 3.11
- FastAPI
- PostgreSQL
```

## ⚙️ 自动修复

如果生成的Markdown有错误，AI应该：

1. 检查标题前后是否有空行
2. 检查代码块前后是否有空行
3. 检查列表前后是否有空行
4. 确保文件末尾有换行符
5. 删除多余的连续空行（保留最多2个）

## 🎓 学习资源

- Markdownlint规则：https://github.com/DavidAnson/markdownlint/blob/main/doc/Rules.md
- Markdown指南：https://www.markdownguide.org/
- CommonMark规范：https://commonmark.org/

## ✅ 验证方法

生成Markdown后，在Cursor中：

1. 保存文件（触发自动格式化）
2. 查看编辑器左侧是否有警告图标
3. 如果有错误，根据提示修正
4. 重新保存直到没有错误

---

**重要提醒**：本规则适用于所有 `*.md` 文件，AI必须在生成时就遵守，而不是事后修复。
